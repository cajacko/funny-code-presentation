{
  "name": "my-presentation",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase-tools": "^3.18.4",
    "ncp": "^2.0.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "rimraf": "^2.6.2",
    "spectacle-scripts": "2.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "docs": "yarn build && rimraf docs && ncp build docs",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "login": "firebase logout && firebase login",
    "deploy": "yarn run build && firebase deploy"
  }
}
